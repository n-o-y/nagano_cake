<h1>ジャンル一覧・追加</h1>
<%= form_with model: @genre, url: "/admin/genres", local:true do |f| %>
  <%= f.text_field :name %>
  <%= f.submit "新規登録" %>
<% end %>

<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>