<h1>注文履歴一覧</h1>
<% @orders.each do |order| %>
  <%= order.created_at %>
  <%= order.postal_code %><%= order.address %><%= order.name %>
  <%= order.id %>
  <%= order.total_payment %>
  <%= order.status %>
  <%= link_to "表示する", "/orders/#{order.id}" %>
<% end %>