<h1>会員一覧</h1>
<% @customers.each do |customer| %>
  <%= customer.id %>
  <%= customer.last_name %><%=customer.first_name %>
  <%= customer.email %>
  <% if customer.is_active == TRUE %>有効
  <% else %>退会
  <% end %>
<% end %>