<h1>商品一覧</h1>
<% @items.each do |item| %>
  <%= item.id %>
  <%= item.name %>
  <%= item.price %>
  <%= item.genre.name %>
  <% if item.is_active == TRUE %>販売中
  <% else %>販売停止中
  <% end %>

<% end %>