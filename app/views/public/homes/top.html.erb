<%= render 'public/header_public' %>
<div class="container">
  <div class="row">
    <div class="col-8 mx-auto mt-5 text-center">
      ようこそ、ながのCAKEへ！</br>
      このサイトは、ケーキ販売のECサイトになります。</br>
      会員でない方も商品の閲覧はできますが、</br>
      購入には会員登録が必要になります。
    </div>
  </div>
  <div class="row">
    <h3 class="col-8 mx-auto font-weight-bold">新着商品</h3>
  </div>
  <div class="row">
    <% @new_items.first(4).each do |new_item| %>
      <div class="col-2 mx-auto d-flex flex-column">
        <%= link_to "/items/#{new_item.id}" do %>
          <%= attachment_image_tag new_item, :image, format: "jpeg", size: "200x150", fallback: "no_image.jpg" %>
          <%= new_item.name %></br>
          ￥<%= new_item.price %>
        <% end %>
      </div>
    <% end %>
  </div>
  <div class="row mx-auto justify-content-end">
    <%= link_to "全ての商品を見る＞", "/items" %>
  </div>
</div>
<%= render 'public/footer_public' %>